# Required. The notebooks install these automatically on first run; this file is
# here for anyone who prefers to prepare the environment up front.
numpy>=1.24
matplotlib>=3.7
pandas>=2.0

# Optional. Every one of these unlocks a path that is otherwise skipped, and
# nothing in the notebooks fails without them.
#
#   boto3                  AWS Bedrock: Knowledge Base retrieval, Titan embeddings,
#                          Converse generation, rerank, and a model judge
#   sentence-transformers   a real neural encoder and cross-encoder in place of the
#                          offline LSA / logistic-regression stand-ins
#   anthropic               the Claude API directly, with prompt caching on the prefix
#
# boto3>=1.34
# sentence-transformers>=2.6
# anthropic>=0.40

# Only needed to execute the notebooks headlessly (CI, or the verification script).
# jupyter>=1.0
# nbclient>=0.9
